def hello(): # Define a function named hello
    print("Hello")

hello() # Call the function to verify it works

def add(a, b): # a & b are parameters
    result = a + b
    return result

ans = add(3, 5) # 3 & 5 are arguments
print(ans)
 